/*
new language: parser, 
expressions: value, word, name, apply
string, number, binding,  
applications:
do(define(x, 10))
if(>(x, 5)) {
    print("large"),
    print("small")
}
*/


function parseExpression(program) {
    return program
}

