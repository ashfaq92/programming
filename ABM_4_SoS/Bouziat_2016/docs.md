# Cooperative Agent Algorithm
- Each agent chooses actions that minimize the maximum criticality of its neighbors
# Notes
1. Add corresponding `Resource` **before** adding `ResourceGoals`.
# Assumptions
1. For a `ComponentSystem` the `ResourceGoal` must be from the corresponding `Resources`.
TODO:
        assumption: we are talking about resource goals
# TODO:
1. Implement NEQ
2. link and goal link similarity?
3. optimal value of `a`
